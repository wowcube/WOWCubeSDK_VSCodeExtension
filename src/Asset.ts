import * as vscode from "vscode";
import * as os from 'os';
import * as fs from 'fs';

export class Asset
{
    public path:string = "";
    public alias: string = "";
    public encoding: string="";  
}